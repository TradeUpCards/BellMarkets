"use client";

/**
 * MarketsTable — empty scaffold. Visual / layout work is **paused** pending
 * Tate's mockup-lock decision. When the mockup is picked, this component
 * composes against `useAllMarkets()` + per-row `useOrderBook(...)` for the
 * implied probability + countdown.
 */
export function MarketsTable() {
  return null;
}
