"use client";

/**
 * PositionList — empty scaffold. Layout (open positions table + redeem-all
 * for settled winners per `docs/DESIGN.md` §/portfolio) is **paused**
 * pending mockup-lock. When picked, composes `useAllMarkets()` + per-market
 * `usePosition(wallet, marketPda)` filtered to non-zero balances.
 */
export function PositionList() {
  return null;
}
