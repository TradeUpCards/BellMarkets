"use client";

/**
 * TradePanel — empty scaffold. Layout (4-button atomic Buy Yes / Sell Yes /
 * Buy No / Sell No per `docs/DESIGN.md`) is **paused** pending Tate's
 * mockup-lock. When the mockup is picked, this composes
 * `useBuyYes` / `useSellYes` / `useBuyNo` / `useSellNo` + `usePosition`.
 */
export function TradePanel() {
  return null;
}
