"use client";

/**
 * OrderBook — empty scaffold. Depth-bar viz + Yes/No perspective columns
 * are **paused** pending mockup-lock. When picked, composes against
 * `useOrderBook(phoenixMarketPda)` and renders `MonoNumber` for each price/
 * size pair with tabular alignment.
 */
export function OrderBook() {
  return null;
}
