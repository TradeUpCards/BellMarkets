{
  "name": "bell-markets",
  "private": true,
  "version": "0.0.0",
  "description": "BellMarkets — non-custodial Solana dApp for daily MAG7 binary outcome contracts (monorepo root).",
  "license": "Apache-2.0",
  "packageManager": "pnpm@9.12.1",
  "engines": {
    "node": ">=20.10.0",
    "pnpm": ">=9.12.0"
  },
  "scripts": {
    "test": "pnpm -r --if-present test",
    "typecheck": "pnpm -r --if-present typecheck",
    "build": "pnpm -r --if-present build",
    "lint": "pnpm -r --if-present lint"
  },
  "devDependencies": {
    "typescript": "^5.5.4"
  }
}
